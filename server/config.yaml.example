mongodb:
  uri: "mongodb://xb3216:2YDfRuQxb0T@192.168.0.233:27017,192.168.0.234:27017,192.168.0.235:27017/?replicaSet=rs0&authSource=admin"
  database: "3216_amazon"

# Separate MongoDB deployment powering the pending-pagesaves widget --
# a completely different connection string than mongodb: above.
pending:
  uri: "mongodb://admin:tP_kc8mn@192.168.2.145:27017/?authSource=admin"
  database: "3216_amazon"
  collection: "Amazon_scope_2_testing_final"

api:
  host: "0.0.0.0"
  port: 8000

auth:
  username: "admin"
  password: "Xbyte@123"
  jwt_secret: "R8@9xL1#AmazonScope2JWT2026!Server"
  jwt_expiry_minutes: 480   # NOTE: was 500000000 (~951 years) -- not currently
                             # causing a crash, but that's an unreasonable
                             # value for a real deployment; set back to a
                             # sane 8-hour default. Change if you disagree.

heartbeat:
  warning_after_missed_intervals: 2
  offline_after_missed_intervals: 4
  expected_interval_seconds: 30

alerts:
  webhook_url: ""

retention:
  days: 1

excel_accounts:
  enabled: true
  file_path: "Accounts.xlsx"
  sheet_name: "Sheet1"
  header_rows: 1
  cache_seconds: 30